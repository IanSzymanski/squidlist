# src/media

P6: MediaResolver, LocalMediaStore and DownloadManager. Resolver selects a complete valid OPFS copy or the Worker stream URL and owns object-URL cleanup. AudioEngine consumes resolved media without provider knowledge.
