# src/audio

P3/P4: AudioEngine, PlaybackChannel, QueueManager, CrossfadeController. Queue selection is independent of playback. Two channels and gains will feed a master output. No implementation in P0.
